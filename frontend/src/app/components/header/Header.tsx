import React from 'react';
import './Header.scss';

const Header: React.FC = () => (
  <header className="header">
    <h1 className="header__title">Medical AI Assistant</h1>
  </header>
);

export default Header;